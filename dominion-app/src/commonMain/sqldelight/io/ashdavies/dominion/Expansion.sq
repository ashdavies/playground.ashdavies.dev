CREATE TABLE expansion(
    title TEXT NOT NULL PRIMARY KEY,
    image TEXT NOT NULL,
    art TEXT DEFAULT NULL
);

selectAll:
    SELECT * FROM expansion;

insertOrReplace:
    INSERT OR REPLACE INTO expansion VALUES ?;

deleteAll:
    DELETE FROM expansion;