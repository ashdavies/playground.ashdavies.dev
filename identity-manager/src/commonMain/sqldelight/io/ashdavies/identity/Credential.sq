CREATE TABLE credential (
    uuid TEXT NOT NULL PRIMARY KEY,
    profile_picture_url TEXT
);

selectAll:
    SELECT * FROM credential;

insertOrReplace:
    INSERT OR REPLACE INTO credential VALUES ?;
